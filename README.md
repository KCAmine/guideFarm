# guideFarm Installation 

1. Install npm and git

2. run git clone : "https://github.com/KCAmine/guideFarm.git"

3. Initialize with npm install (make sur you're in the right directory)

4. Run `npm run docs:dev ` 

5. Any file changes should automatically apply to the site 

